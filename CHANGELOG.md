# 0.5.2.0 - 2026-09-22

## What changed

- Name both versions in the update prompt, and title it after the plugin (#25)
- Release 0.5.2.0 (#26)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.5.1.0 - 2026-09-22

## What changed

- Say it plainly: a new version is available, update it now (#23)
- Release 0.5.1.0 (#24)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.5.0.0 - 2026-09-22

## What changed

- Write down that the ACT site lags GitHub (#20)
- Confirm an update happened, twice, so neither half can be missed (#21)
- Release 0.5.0.0 (#22)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.4.2.0 - 2026-09-22

## What changed

- Reload the plugin the way ACT's own EQ2 parser does (#18)
- Release 0.4.2.0 (#19)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.4.1.0 - 2026-09-22

## What changed

- Show the same four-part version everywhere the plugin prints one (#14)
- Stop renaming ACT's own record of where the plugin lives (#15)
- Reload the plugin in place instead of asking for ACT to be restarted (#16)
- Release 0.4.1.0 (#17)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.4.0.0 - 2026-09-22

## What changed

- Say which of the two things went wrong when a download fails (#11)
- Stop rejecting a download that succeeded (#12)
- Release 0.4.0.0 (#13)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.3.0.0 - 2026-09-22

## What changed

- Install the downloaded build instead of leaving it wherever ACT put it (#9)
- Release 0.3.0.0 (#10)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.2.0.0 - 2026-09-22

## What changed

- Put every file under ACT's own folder and switch the updater on with plugin id 102 (#7)
- Release 0.2.0.0 (#8)

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.

# 0.1.0.0 - 2026-09-18

## What changed

- EQ2 Loot Atlas: an ACT plugin that records where items come from
- Stop sub-area announcements creating phantom zones
- Commit the ignore rules so they apply to every clone
- Add the release script and the public downloads repository
- Take the release tag from the built DLL rather than restating it
- Attach the plugin to the release instead of committing it
- Stop a routine git warning from killing the release mid-publish

Install by dropping `EQ2LootAtlas.dll` into ACT's plugin folder. Setup instructions are at <https://lootatlas.noviets.com/setup>.









